# Model Evaluation: claude-sonnet-4-6

**Date:** 2026-08-21 01:20:50

## Prompt

```
Write a Python script that connects to Chrome over CDP on port 19333 and lists all open tabs.
```

## Response

Python Chrome DevTools Protocol CDP connect list tabs websocket 2024 5 results

I have all the information I need. The CDP HTTP endpoint /json lists all open tabs. I'll now write the Python script. Note that this is a Python script, not a website — I'll create it as a .py file.

Created list_tabs.py

The script list_tabs.py has been created. Here's a full breakdown:

How it works

Chrome exposes a lightweight HTTP API as part of CDP. The script uses two endpoints — no third-party libraries needed:

Endpoint	Purpose
GET http://localhost:19333/json	Lists all open tabs / targets
GET http://localhost:19333/json/version	Returns browser metadata
Usage
1. Start Chrome with remote debugging enabled
Bash
# Linux / macOS
google-chrome --remote-debugging-port=19333

# macOS (full path)
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=19333

# Windows
chrome.exe --remote-debugging-port=19333
2. Run the script
Bash
python3 list_tabs.py
3. Optional: dump raw JSON
Bash
python3 list_tabs.py --json
Sample output
text
Connecting to Chrome CDP at http://localhost:19333 …
Browser         : Chrome/125.0.6422.112
Protocol version: 1.3
Browser WS URL  : ws://localhost:19333/devtools/browser/abc123

Found 3 target(s) total.

──────────────────────────────────────────────────────────────────────
  INDEX   TITLE                                URL
──────────────────────────────────────────────────────────────────────
  1       GitHub                               https://github.com
          WebSocket URL:                       ws://localhost:19333/devtools/page/AAA

  2       Google                               https://www.google.com
          WebSocket URL:                       ws://localhost:19333/devtools/page/BBB
──────────────────────────────────────────────────────────────────────
  Total page tabs : 2
  Other targets   : 1
──────────────────────────────────────────────────────────────────────
Key details
No dependencies — uses only Python's standard library (urllib, json).
Filters by type — separates page targets (real tabs) from other targets like service workers, extensions, and DevTools panels.
WebSocket URLs — each tab's webSocketDebuggerUrl is printed, which you can use to send CDP commands directly to that specific tab.
Error handling — gives a clear message if Chrome isn't reachable on the specified port.
Deployed the project
Show More




01a020b9-144c-7ad7-b0ea-af40...arena.site
